import { AnnotationsFilterPipe } from './annotations-filter.pipe';

describe('AnnotationsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnotationsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
